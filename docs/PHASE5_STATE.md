# Phase 5 — Running Implementation State

Updated at each ticket acceptance. Authority: `PLAN_MASTER.md` (what/why) →
`docs/PHASE5_BUILD_PLAN.md` (how) → `docs/PHASE5_RECON.md` (findings, traps,
per-ticket plans). This file is the sprint's status board, not a design doc.

---

## Test status

| Gate | Value | Notes |
|---|---|---|
| Engine regression baseline | **229** | Phases 1–3, must never drop |
| Current suite | **538 passed, 13 skipped** | 229 engine + backend (Tickets #1/#2/#5/#5b/#6); skips are real-dataset tests gated on `datasets/` presence. +50 over the previous 357: Ticket #5 added `tests/test_streaming_scorer.py` (39 tests, 0 skipped — real dataset present on this machine, including the 5 real-data tests; 3 tests added post-review for HIGH-1/LOW-1, see the Ticket #5 row) plus 11 additive tests in `tests/test_backend_config.py` for the 4 new `BACKEND_SETTINGS` fields. +41 over the previous 407: Ticket #7 added `tests/test_ingest.py` (44 tests; 3 are live-Postgres tests gated on `AEGIS_TEST_LIVE_DB=1` and skipped by default, all 3 verified passing against the local DB). +35 over the previous 448: Ticket #8 added `tests/test_api.py` (35 tests, 0 skipped — the DB-backed routes run against an in-memory SQLite bound to the real `Base.metadata`, so no Postgres is needed in CI; 5 of the 34 were added in the post-review fix round for HIGH-1/LOW-1, and 1 further test, `test_events_catch_up_survives_id_and_ts_order_disagreement`, was added after acceptance for HIGH-2 — see the Ticket #8 row) |
| Ruff (`src/ backend/`) | clean | `E,F,W`, ignore `E501` |
| Duplicate-def check (`src/*.py`) | clean | CI parity |
| Invariant A (`src/` untouched) | **holding** | verified per ticket via `git status --short src/` |

---

## Tickets

| # | Ticket | Owner lane | Status |
|---|---|---|---|
| 1 | Postgres + environment/config | A | ✅ **ACCEPTED** |
| 2 | Schema + models + asset seeding | A | ✅ **ACCEPTED** |
| 3 | Next.js scaffold + `docs/DESIGN_CONSOLE.md` theme + app shell | B | ✅ **DONE — accepted after review.** New theme ("Warm Industrial Glass": warm charcoal grounds, amber brand accent, translucent glass panels) supersedes `docs/DESIGN.md` for this console only; `DESIGN.md` is unchanged and still governs the Streamlit Research Console. Layout reworked from `PHASE5_BUILD_PLAN.md` §8: alerts are a column (room for the `explain()` "why" text), graph is the hero region. Review found and fixed 2 MEDIUM findings, both connection-state defects: header and graph panel could show contradictory connected/disconnected state at once (health polled and recovered, topology was a one-shot fetch stuck forever), and the error copy claimed "retrying" while the network log proved nothing was retrying. Fixed with one shared `ConnectionProvider` owning the health poll; data panels refetch on reconnect. Verified live across connect→kill→restart with no page reload, network log proving a fresh 200 topology request after recovery. Zero raw hex in components (tokens only); `src/`/`backend/` untouched; backend suite unchanged at 483 passed |
| 4 | Mock WebSocket server + stream client | B | ✅ **DONE.** `frontend/scripts/mock-ws-server.mjs` on `:8001/ws/stream`, drop-in for the real Ticket #9 endpoint — same path, same `{"type","data"}` envelope, payload keys diffed mechanically against `backend/ingest.py`'s `_broadcast_batch`/`_handle_anomalies` (event 18 keys, alert 9, cii 7, exact match). Emits plausible data: real asset names fetched from live `GET /api/topology` at startup (hardcoded fallback so the mock still runs standalone), `192.168.10.x`/`10.0.1.x` IPs per K8, ~2% anomaly rate, alerts rare and mostly critical per the P5-15 alert policy. Stream status kept deliberately separate from API status (two independent transports) — verified live: killing the mock leaves the header API indicator `CONNECTED` while the stream flips to `RECONNECTING`; backoff confirmed real (1 reconnect attempt in a 12s window, no tight loop); restarting the mock self-heals to `LIVE` with no page reload. `ws` is a devDependency only, never in the prod bundle. Header's `events/s`/`alerts` chips now live; feed row rendering stays Ticket #10's job |
| 5 | `StreamingScorer` + supervised detector channel + tests | A | ✅ **ACCEPTED after review** — review found 1 HIGH + 1 MEDIUM + 1 LOW finding, all fixed and re-verified. **HIGH-1:** `backend/supervised_detector.py` called `register_detector()` at import time, polluting the global `detectors.registry.DETECTORS` (contract: "registered ⇒ benchmarked"); `evaluation.run_evaluation()` then fit the supervised classifier label-free on a benign-only split and published a garbage all-zero `Supervised_Flow` row into the Research Console's benchmark table. Fixed by never registering it: the detector is now exposed only via a backend-local `BACKEND_DETECTORS` dict, and the `fit(X, y=None)` silent always-"normal" fallback (which existed only to survive that registration) was removed — `fit()` without labels now raises `ValueError` naming `fit(X, y)`/`temporal_split_evaluate()`; `predict()`/`score_samples()` likewise raise if called before a real fit. Verified: importing `backend.supervised_detector` leaves `DETECTORS` unchanged at `['isolation_forest', 'mad', 'ocsvm', 'tripwire', 'zscore']`, and `run_evaluation()` produces no `Supervised_Flow` row. See K6. **LOW-1:** `backend/streaming.py`'s `explain()` took a bare `ReplayFlow` and leaked `AttributeError: 'ReplayFlow' object has no attribute 'flow'`; added a type guard raising a clear `TypeError` naming both types and `score_batch()`/`score_event()`. Both fixes covered by new tests in `tests/test_streaming_scorer.py` (`test_not_registered_in_global_c3_registry`, `test_exposed_via_backend_local_map`, `test_fit_without_labels_raises`, `test_predict_before_fit_raises`, `test_explain_rejects_replay_flow`); `temporal_split_evaluate("friday-morning")` re-verified unchanged (AUC 0.847 / P 0.996 / R 0.595). `backend/streaming.py` (`StreamingScorer`, `fuse_tripwire_confidence`), `backend/warmup.py` (build CLI), `backend/supervised_detector.py` (`SupervisedFlowDetector`, `temporal_split_evaluate`, `BACKEND_DETECTORS`); additive extension of `backend/replay_reader.py` (4 new `ReplayFlow` fields) and `backend/config.py` (4 new `BACKEND_SETTINGS` fields); `tests/test_streaming_scorer.py` (39 tests) + 11 additive tests in `tests/test_backend_config.py`. **MEDIUM-1 (found by Opus in final verification):** `test_save_uses_resolved_path` wrote to the repo's real `artifacts/streaming_scorer.joblib` and then unconditionally `unlink()`ed it, destroying any warmup artifact the operator had built. Demo-day failure mode: build artifact → run the test suite (as one should before demoing) → artifact silently gone → the console fails at startup with a missing-artifact error. Fixed by preserving and restoring any pre-existing artifact in a `finally` block, deleting only when the test itself created the file. Verified both branches: with a pre-existing artifact it survives byte-identical (md5 unchanged across a full run); with none, nothing is left behind. See K7. Post-fix: **407 passed, 10 skipped**; ruff clean; `src/` untouched. See P5-13, P5-14 below |
| **5b** | **`ReplayFlowReader` (real IP/timestamp source)** | A/C | ✅ **ACCEPTED** |
| 6 | Replay engine | C | ✅ **ACCEPTED** — review found 2 MEDIUM findings (unbounded batch size scaling with speed; `emitted_count` not distinguishing consumer failures), both fixed and re-verified (P5-12). `backend/replay_engine.py` + `tests/test_replay_engine.py`; 34/34 tests pass; 357 passed / 10 skipped overall; ruff clean; `src/` untouched |
| 7 | `backend/ingest.py` — score -> persist -> broadcast, CII debounce | A | ✅ **DONE** — `IngestPipeline` is a drop-in `replay_engine.Consumer`. Verified end-to-end against the real engine + real fitted scorer + real Postgres: 5,000 friday-morning flows at 2000x in 4.3s, `consumer_errors=0`, 5,000 events + 5,001 event_scores persisted, insert order chronological, 1 tripwire alert raised / 84 volumetric suppressed. Found and fixed a real bug during implementation (see P5-16). Surfaced K8. 6 new `BACKEND_SETTINGS` fields; wires `prune_events()` per Ticket #2's deferral |
| 8 | FastAPI REST routes — the nine routes in docs/PHASE5_TICKET8_PLAN.md | A | ✅ **DONE — accepted after review, THEN a second HIGH found and fixed by live end-to-end verification.** Opus review found 2 findings (0 CRITICAL/HIGH-severity-implementation-bug, both listed below), both fixed and re-verified. **HIGH-1:** the plan's own D8-2 was wrong, not the implementation — `GET /api/events?since=` combined a `since`-bounded query with `ORDER BY ts DESC, id DESC`, which silently and *permanently* loses events on catch-up. Measured on real Postgres (250 events sharing one `ts`, ids 5067..5316, cursor at 5067, `limit=100`): poll 1 returned the newest 100 rows (5217..5316) and advanced the cursor there; every following poll returned 0 rows; 149 of 249 events were never delivered, with nothing in the response to reveal the loss. Fixed: ordering is now conditional — `since` omitted keeps `ts DESC, id DESC` (page load, unchanged), `since` supplied flips to `ts ASC, id ASC` (catch-up, new) so a client drains its backlog forward with no gap; the `id` tie-break is preserved in both directions. `EventsResponse` gained `has_more: bool` (fetch `limit + 1`, trim, never guessed) so a client can tell it is still behind. See decision P5-18 below and the correction note in docs/PHASE5_TICKET8_PLAN.md section 4. **LOW-1:** `/api/health` captured `AppRuntime.scorer_load_error` but never surfaced it, so an operator saw `scorer_loaded: false` with nothing to act on; added `scorer_load_error: Optional[str]` to `HealthResponse`, populated from the runtime, `None` when loaded. Both fixes covered by new tests: `test_events_catch_up_polling_drains_every_tied_timestamp_event_with_no_gap` (the acceptance test — verified by hand to fail under a temporary revert to unconditional DESC ordering, reproducing exactly the missing-middle-range failure), `test_events_since_uses_ascending_order_page_load_uses_descending`, `test_events_has_more_true_when_backlog_exceeds_limit_false_on_last_page`, plus an updated `test_events_since_is_exclusive_id_lower_bound_not_a_timestamp` (ordering assertion flipped to ascending); `test_health_reports_scorer_load_error_message`, `test_health_scorer_load_error_is_none_when_scorer_loaded`. Files: `backend/routes.py`, `backend/schemas.py`, `backend/runtime.py` (unchanged — already had the field), `tests/test_api.py`; docs/PHASE5_TICKET8_PLAN.md (correction note, section 4), this file. `git status --short src/` empty throughout (Invariant A). **HIGH-2 (found AFTER acceptance, by live end-to-end verification against a running uvicorn server + real Postgres):** the HIGH-1 fix was correct as far as it went but incomplete — it made the catch-up branch ascending, but sorted it by `ts ASC, id ASC` while the filter stayed `WHERE id > :since`, i.e. the filter key and the sort key were different columns. That is only safe if id order and ts order always agree, and a replayed capture day proves they do not: a second replay session gets higher ids than the first but can land in an earlier or overlapping `ts` range. Measured on real Postgres, three replay sessions of one day: session A ids 5062..5066 (ts 14:29..14:30), session B ids 5567..11566 (ts 14:29..14:41), session C id 11567 alone (ts 14:29 — earliest ts of the three, but the highest id). `since=10567&limit=5` sorted id 11567 to the front of the page (earliest ts), so a client advancing its cursor to `max(id)` skipped ids 10568..11566 forever, or (advancing by sort-order position instead) re-received id 11567 on every poll forever. Draining from a cursor below every event: **200 of 6006 events delivered — 96.7% silently lost**, worse than HIGH-1. Fixed by making the catch-up sort key identical to the catch-up filter key: `ORDER BY id ASC` alone (no `ts` in the catch-up sort at all), so the drain is gapless by construction; the page-load branch (`since` omitted) is unchanged. See decision P5-18's addendum below and the second correction note in docs/PHASE5_TICKET8_PLAN.md section 4. New regression test `test_events_catch_up_survives_id_and_ts_order_disagreement` (verified by hand to fail under the `ts ASC, id ASC` catch-up ordering — a fixture with a later-timestamp batch inserted first and one earliest-timestamp row inserted last so id order and ts order disagree; pre-fix it delivered only ids {2, 3, 7} of the expected 2..7 and then stopped); updated `test_events_since_uses_ascending_order_page_load_uses_descending`'s docstring to explain why its all-tied-`ts` fixture cannot by itself distinguish `id ASC` from `ts ASC, id ASC` (both produce the same sequence there) and point at the new test for the id-vs-ts distinction. Files: `backend/routes.py` (route docstring + `since` field description rewritten, `order` tuple simplified to `(Event.id.asc(),)` for the catch-up branch), `tests/test_api.py`; docs/PHASE5_TICKET8_PLAN.md (second correction note, section 4), this file. `git status --short src/` empty throughout (Invariant A) |
| 9 | `WS /ws/stream` — real WebSocket endpoint | A | ✅ **DONE.** `backend/ws_broadcaster.py` implements the `Broadcaster` protocol Ticket #7 defined. **Resequenced ahead of #10** on an explicit instruction that no synthetic data may appear anywhere: building the feed against Ticket #4's mock would have baked fake events into the demo path. This also restores Invariant E. **D9-1 (the hard part):** `IngestPipeline._safe_publish()` calls `publish()` synchronously from the `ReplayEngine`'s background thread, but WebSockets are asyncio objects owned by the loop — handled by capturing the loop once in the lifespan and handing off via `loop.call_soon_threadsafe`, never awaiting or blocking in the ingest thread (a blocking publish would stall the replay engine and desynchronise the stream). Proven by tests driving `publish()` from real `threading.Thread`s, single and 10-concurrent. **D9-2:** per-client bounded `asyncio.Queue` (`ws_client_queue_max`, default 1000), drop-oldest on overflow with a counted `dropped` metric, one writer task per connection — a slow browser tab can never become the replay engine's rate limiter. **Absorbs Ticket #12:** frontend default WS URL flipped from the mock (`:8001`) to the real endpoint (`:8000/ws/stream`); the mock survives as an opt-in reconnect-test fixture only. Verified end-to-end with REAL data: 767 event envelopes received over the real socket during a live friday-morning replay, real capture IPs (192.168.10.x, 13.78.188.147), `source_dataset = CIC-IDS2017-TrafficLabelling` cross-checked in Postgres, `consumer_error_count` 0, lag 0.012s. 2 new `BACKEND_SETTINGS` fields; `tests/test_websocket.py` (11 tests) |
| 10 | Live telemetry feed | B | ✅ **DONE — accepted after review.** `TelemetryRail` renders the real stream. **D10-1:** newest-at-top, top-pinned — the build plan's "autoscroll" implemented without scroll-chasing, which at real replay rates is unreadable and fights the user. **D10-2 freeze-on-interaction** is what makes the feed usable at all: hover/scroll freezes the displayed rows with an honest `PAUSED · N new` badge carrying a real count, while the stream keeps receiving and the header keeps ticking — display-only, and the copy says so. **D10-3:** DOM updates batched (~1 frame) rather than one re-render per message. High-rate test on the REAL path: replay at `speed=500` drove **2000 events/s** with no visible jank and the 200-row cap holding exactly (`emitted_count: 20000, consumer_error_count: 0`). Asset identity falls back to the raw IP when resolution yields `Unresolved_*` (K8), since rendering that string verbatim would fill the feed with noise. **Review found 1 HIGH:** `useEventStream()` was called from three components (`AppHeader`, `StreamState`, `TelemetryRail`), opening **3 WebSockets per browser tab** — 3x fan-out against Ticket #9's per-client queue/writer-task, and worse, the header's counters and the feed's rows arrived on *different sockets with different buffers* and could legitimately disagree. Same class of defect as Ticket #3's contradictory connection state; the lesson is one owner per concern. Fixed with a `StreamProvider`/`useStream()` context owning exactly one `useEventStream()` instance, mirroring the existing `ConnectionProvider`. Verified independently: with one tab open, exactly **1** long-lived socket survives an 8s sampling window (was 5). Transport/reconnect/buffering in `useEventStream` untouched |
| 11 | City graph + `/24` cluster aggregation | B | ✅ **DONE — accepted after 2 review rounds.** **Resolves K8**, open since Ticket #7. The build plan §5 assumed real IPs map onto curated assets; K8 measured that false (0 of 20,000). The tempting fix — inventing edges from cluster nodes into the curated topology so the cascade has somewhere to go — was **rejected**: it would fabricate dependencies that do not exist and make any CII over them meaningless. Instead the graph renders **two visually distinct, genuinely disconnected layers** — the curated city asset model (16 nodes/20 edges, the risk model) and observed `/24` traffic clusters from the live stream — with a caption stating the disconnection outright ("Observed capture traffic (N distinct /24 subnets) does not intersect the modelled city assets — no edges are invented between the two layers"). Turning the limitation into an explicit statement is more credible than hiding it, and Ticket #13's injection names curated assets directly, so the demo's cascade still lands on layer 1. **D11-2:** clustering is incremental and throttled to the render cadence, capped at top-24 by flow count with an `other` rollup — verified holding at 24 of 234-352 observed subnets under a real replay. **Review round 1** found the runaway-canvas fix had clamped the whole document (`overflow-hidden` on body), leaving City Infrastructure a 34px header and Active Alerts clipped with no scrollbar at 1000px — two of three panels unusable; fixed by scoping the fixed-viewport clamp to the `xl` breakpoint and giving the graph panel its own definite height below it. **Review round 2** found the canvas guard (`size.width > 0 && size.height > 0`) had no fallback, rendering a silently blank hero panel — fixed with a synchronous `useLayoutEffect` measurement plus bounded rAF retry, and an honest "sizing graph…" fallback. A real overflow bug (canvas 942px wide inside a 638px holder after a resize) was fixed in the same round; canvas now tracks its holder exactly (638x662). **Known item deferred to #19 (styling pass):** with 24 clusters rendered the force layout is cramped and the two-layer separation is visually lost — functional but not yet legible enough for the demo. Also deferred: below `xl` the telemetry rail loses its internal scroll, so the document grows long (nothing clipped or unreachable) |
| 12 | Frontend swap mock → real WS | C | ✅ **DONE** — absorbed into Ticket #9 |
| 13 | `POST /api/inject` — real attack scenarios | C | ✅ **DONE.** **Reshaped by the no-synthetic-data rule:** the original design (`generate_scripted_attack`) fabricates flows and is deliberately NOT used. Injection instead replays **real captured attack traffic** — verified present: friday-morning `Bot` 1,966 flows, friday-afternoon `DDoS` 128,027, `PortScan` 158,930. **D13-1:** real flows are re-targeted onto the operator's chosen curated asset (source only — `IngestPipeline` computes CII from the *source* resolution), preserving every real characteristic (bytes, packets, duration, protocol, ports, timing, and the real `label`). Necessary because K8 means real capture IPs resolve to nothing in the graph, so CII would be all zeros and the demo's cascade would not render. This is precisely the question the CII engine exists to answer — *if this asset were compromised, what else falls over* — applied to a real observed attack pattern rather than an imagined one. **D13-2:** one scenario sets `is_honeytoken_use` on the real flows; a honeytoken touch cannot exist in a 2017 public capture because it is AEGIS's own planted credential — telemetry stays real, only the deception-layer control flag is set. **Demo payoff demonstrated, not assumed:** honeytoken scenario → tripwire alert (critical, `City_Payment_Gateway`) → CII snapshot median **0.5090** / p5 0.2472 / p95 0.5090, impacted `Gateway_L5 → Bank_Partner_API → Gateway_L4 → Social_Welfare_System` — the payment-gateway→bank→welfare cascade. Flow pool cached per scenario: first load 1.42s, cached 0.0014s (~1000x). Returns **409** when the engine is not running (injected flows would otherwise sit queued and be silently wiped by the next `start()`), **422** for unknown scenario or a target asset lacking a real static IP (accepting a gateway/synthesized node would silently resolve the flow to a different asset), **503** with no scorer. **Review found 1 MEDIUM, fixed:** the live WebSocket `event` envelope carried `batch_index` but not `batch_origin`, so a feed viewer could not distinguish an operator what-if from observed capture telemetry in real time (`events.raw` had it, so REST could, but the feed is the surface an operator watches). Added `batch_origin` to the envelope and an `INJECT` badge on injected feed rows. Verified live: 22 replay vs 5 injected cleanly separated over the socket. `backend/inject.py`, `tests/test_inject.py` (16) + 9 in `tests/test_api.py` |
| 14 | CII cascade animation + graph legibility | B | ✅ **DONE — accepted after review.** Pulled Ticket #11's deferred legibility work forward, because animating a cascade across an illegible graph is pointless. **D14-1:** the curated topology is 16 nodes with a fixed structure that never changes at runtime, so it is laid out deterministically (Purdue level x criticality, with barycentre y-ordering to shorten edges) and **pinned** via `fx`/`fy`; clusters stay force-driven inside their own confined region and can no longer shove the city model into a knot. A stationary curated layer is also what makes the cascade readable. **D14-2:** the animation is driven strictly by the real `cii` envelope's `impacted` array — **no scripted path**, since a cascade animation that did not reflect the actual Monte Carlo output would be exactly the fabricated-signal problem refused in P5-15/D8-3/D11-1. Median is shown **with** the p5–p95 interval, because CII is a distribution, not a point estimate. Verified live: envelope `impacted: [Gateway_L5, Bank_Partner_API, Gateway_L4, Social_Welfare_System]`, median 0.509 / p5 0.247 / p95 0.509 — UI banner and highlighted nodes matched exactly. Two injects 0.3s apart: the second replaced the first with no backlog. **Review found 1 HIGH:** curated labels still collided illegibly (598px canvas, ~6 Purdue columns ~100px apart, labels 100-150px wide — overlapping by construction), defeating the demo's whole point of pointing at Payment Gateway → Bank API → Welfare System. Fixed with greedy sequential label placement (nearest non-intersecting ring, checked against every already-placed box), deterministic token-boundary truncation with full names on hover, and token-coloured backing plates. Notably the implementer tried the simpler 'alternate above/below by column parity' first, **measured it still overlapping**, and replaced it rather than declaring it fixed. All 16 labels verified readable. **Deferred to #19:** the camera frames the curated layer, so the 24 cluster nodes sit out of view — the two-layer K8 narrative needs both visible at once |
| 16 | `GET /api/stats` — header counters + risk index | A | ✅ **DONE.** Mostly plumbing over the 12 counters `IngestPipeline.stats()` already tracked, plus replay status and DB-sourced alert counts (deliberately not the in-memory counter — an operator restart must not reset what the panel shows). **D16-1, the only non-plumbing part:** "risk index" was undefined in the build plan, and a number in a header labelled RISK reads as authoritative, so inventing one to fill the slot would be the fabricated-signal problem refused in P5-15/D8-3/D11-1/D13-1. Defined explicitly as `Σ(severity_weight × asset_criticality)` over **unacknowledged** alerts, normalised against a settings constant whose docstring states it is a presentation scale, not a calibrated probability — and surfaced in a UI tooltip so it cannot be mistaken for a standard measure. Deliberately **not** built on CII: measured across all 50 assets, 28 report exactly 0.0 and 18 exactly 1.0, so CII is near-binary and would propagate that degeneracy into the first number an operator reads. Unacknowledged-only is what makes acking visibly reduce risk. Verified independently end to end: **risk 0 → 19 on inject, 19 → 0 on ack** (formula check: 100 × 1.0 × 0.95 / 5.0 ≈ 19). **D16-2:** server owns cumulative totals, client owns the live per-second rate — the server returns no rate field at all, so the header can never show two disagreeing "events/s" (the defect class already hit in #3 and #10). **D16-3:** `alerts_suppressed` is surfaced with an explaining tooltip, making the alert policy visible rather than hidden — live reading was 265 suppressed against 1 created, i.e. the system transparently reporting what it saw and chose not to page. 4 new settings; +20 tests |
| 15–21 | — | — | not started |

---

## Environment (provisioned, verified)

- **PostgreSQL 16.15** (Homebrew), running as a service, auto-starts at login
- Role `aegis` / password `aegis` / database `aegis` on `127.0.0.1:5432`
- Binaries: `/opt/homebrew/opt/postgresql@16/bin` (on `PATH` via `~/.zshrc`)
- Verified: TCP connect as `aegis`, plus CREATE/INSERT/SELECT/DROP round-trip
- Disk: ~160 GB free (was 2.5 GB — resolved before Ticket #1)
- Backend deps installed into the existing `venv/`: FastAPI 0.141, SQLAlchemy 2.0.52,
  psycopg 3.3.4, pydantic-settings 2.15, uvicorn, httpx, pytest-asyncio, joblib

---

## Architectural decisions taken during implementation

| # | Decision | Rationale |
|---|---|---|
| P5-1 | `backend/` is a new top-level package, not inside `src/` | Keeps Invariant A mechanically checkable (`git status src/`) and keeps `backend/` outside the CI duplicate-def walk, which only scans `src/*.py` |
| P5-2 | `backend/__init__.py` puts `src/` on `sys.path` once | Matches the repo's existing flat-import convention (no `setup.py`/`pyproject.toml`); lets later tickets `from core.pipeline import run_analysis` without duplicating engine logic |
| P5-3 | `requirements-backend.txt` separate from `requirements.txt` | The engine's dependency set stays untouched; the Research Console keeps working even if backend deps change |
| P5-4 | Config split: `src/settings.py` = engine tuning (no env), `backend/config.py` = deployment (env + `.env`) | Preserves the existing "engine reads no environment" property while giving the backend a conventional 12-factor surface |
| P5-5 | Tiered replay timing: Monday = genuine seconds, Tue–Fri = interpolated minute buckets, every event tagged `timing_provenance` | PCAP replay ruled impractical (see recon §0.5); honours "prefer genuine timing whenever practical" without fabricating precision |
| P5-6 | `api_host` defaults to loopback | API/WebSocket have no auth (out of scope per PLAN_MASTER §15); binding publicly would expose replay + injection control on venue wifi |
| P5-7 | File order is not chronological (Monday 39.41% / Wednesday 23.66% inversions; CICFlowMeter emits in flow-completion order). The reader always sorts by corrected timestamp, stably, so equal timestamps keep original file order | The only defensible within-bucket ordering — minute-granularity files offer no finer ordering signal than file order for ties |
| P5-8 | Landing day is `friday-morning` (1.03% real Bot traffic, realistic); Monday is warmup-only (0.0% attacks, genuine second resolution); Friday-afternoon rejected (55–57% attack traffic is unrealistic) | A landing stream with zero real anomalies undercuts a live demo (Invariant E); 55–57% attacks is implausibly hostile for an operations console |
| P5-9 | `ReplayFlowReader` yields raw IPs and does not resolve assets | Resolution and `/24` clustering belong to Tickets #7/#11 — keeps this reader single-purpose and independently testable |
| P5-10 | `ReplayEngine` (`backend/replay_engine.py`) emits micro-batches (`consumer(batch: list[ReplayFlow], meta: BatchMeta)`), never single flows | Ticket #6 brief's Fact B, measured on this machine: per-event scoring+insert costs 1.78 ms/event against a 0.747 ms/event budget at friday-morning's peak bucket density (4,017 events/3s at 20x) — **2.4x over budget**. Batched scoring (500) + `executemany` insert costs ~0.019 ms/event, ~40x under budget. Batching also caps WebSocket frames/sec for Ticket #9 |
| P5-11 | Within-bucket pacing uses a separate `compute_virtual_times()` interpolation, purely for scheduling; `ReplayFlow.ts` is never mutated | Verified failure mode without this (T3): naive delta-pacing on friday-morning's densest bucket (4,017 events sharing one timestamp) fires all 4,017 in one instant, then idles ~3s. Measured proof (Ticket #6 verification run): naive burst = 4,017 vs. engine's actual max batch size = 134 at the documented speed=20x/tick=100ms. `timing_provenance` (P5-5, `backend/models.py`) remains the sole honest record of which timing tier a row came from; the interpolated virtual time is never written back into `ts` or exposed as an observed time |
| P5-13 | `StreamingScorer` lives at `backend/streaming.py`, not `src/core/streaming.py` (PLAN_MASTER's original location) | docs/PHASE5_TICKET5_PLAN.md section 9, verified two ways: (1) K2 requires reading `BACKEND_SETTINGS.model_artifact_path_resolved`, but a `src/` module cannot import `backend.config` — `backend/__init__.py` establishes `backend -> src`, nothing establishes the reverse, and `PYTHONPATH=src python -c "import backend"` raises `ModuleNotFoundError` from any CWD but the repo root; (2) a new untracked file under `src/` would make `git status --short src/` non-empty, breaking the one mechanical Invariant-A check every ticket has been accepted against. Note: the CI duplicate-def check does NOT independently justify this — it globs `src/*.py` (top-level only, not recursive), so `src/core/streaming.py` would never have been scanned by it either way |
| P5-14 | Two detection channels ship side by side: `StreamingScorer` (unsupervised IsolationForest, NOVEL-threat channel) and `backend.supervised_detector.SupervisedFlowDetector` (supervised RandomForest, KNOWN-threat channel) | `docs/DETECTION_STUDY.md` (new, decisive): the unsupervised volumetric detector measures ROC AUC 0.600 / precision ~0.02–0.035 on real friday-morning traffic — Bot C2 beaconing is *smaller* than benign traffic (median 6 bytes vs 70), so an outlier detector over volume looks in the wrong direction. The SAME features, fed to a supervised RandomForest, measure AUC 0.9994 / F1 0.9872 (same-distribution) — the signal is present, unsupervised is the wrong paradigm. Honest temporal-split evaluation (train on the earlier half of a day, test on the later half — never same-distribution self-test) reproduced on this machine: **AUC 0.847, precision 0.9959, recall 0.5945, F1 0.7446** on friday-morning (study's own numbers: AUC 0.847, precision 0.9979, recall 0.585 — matches closely). The supervised channel's own hard limit is recorded, not hidden: the study's cross-day/novel-attack-family test scored precision 0.000 — supervised detection catches nothing it has not been trained on. `streaming_contamination` (new `BACKEND_SETTINGS` field, default 0.005) is a stated FALSE-POSITIVE budget on the all-benign warmup slice, deliberately not `SETTINGS.ml.isolation_forest_contamination` (0.08, which would flag ~23 flows/sec of a 20x demo — measured on this machine, matching the plan's table) |
| P5-12 | Emission batch size is capped (`BACKEND_SETTINGS.replay_max_batch_size`, default 500); overflow carries forward to the next tick and is never dropped | Review finding (MEDIUM-1): batch size scales linearly with the operator-controllable speed multiplier — `set_speed()` today, `POST /api/replay/speed` in Ticket #8/#13 — so an unbounded cap hands Ticket #7 an unbounded bulk insert and Ticket #9 an unbounded WebSocket frame. Measured speed→batch-size table (friday-morning, 20,000-flow runs): `speed=20x batches=858 max_batch=87` (unaffected by the cap — the demo path stays exactly as it was), `speed=200x batches=89 max_batch=500` (was 860 uncapped), `speed=2000x batches=41 max_batch=500` (was 3,955 uncapped). All three re-verified no-loss (`total_emitted == requested_limit`) and chronologically ordered with the cap active. The resulting schedule slippage is not hidden: at 2000x, `status().lag_seconds` reported 3.24s at completion — the honest trade of bounded frames plus visible lag instead of unbounded frames. `max_batch_size` is an optional-override constructor parameter on `ReplayEngine`, per the CLAUDE.md convention |
| P5-15 | Alerting is policy-gated, and "anomaly detected" is deliberately NOT "raise an alert". Tripwire -> ALWAYS alert (severity critical). Volumetric-only -> suppressed by default (`BACKEND_SETTINGS.alert_on_volumetric=False`), and even when enabled must clear `alert_volumetric_min_calibrated_score`. Both channels de-duplicated per asset by `alert_asset_debounce_sec` | `docs/DETECTION_STUDY.md` measured the volumetric channel at 5 TP / 811 FP on real friday-morning traffic (precision ~0.02). One alert per volumetric anomaly = ~800 junk rows per replay day, burying the single tripwire alert the demo's headline moment depends on. Reproduced by this ticket's own end-to-end run: 5,000 real flows produced 85 anomalies, of which 84 were volumetric (suppressed) and 1 was the tripwire (alerted). Suppression is **alerting only** — volumetric anomalies are still scored, still written to `event_scores`, and still broadcast on the event channel, so they stay visible in the live feed and counters. Nothing is hidden; it just does not page an operator |
| P5-16 | `IngestPipeline` defaults its resolver to `AssetRegistry.from_config()`, never the bare `AssetRegistry()` constructor | Real bug found and fixed during implementation. `AssetRegistry()` builds an **empty** registry — the seeded one is the `from_config()` classmethod. With the bare constructor every curated smart-city IP falls through to auto-discovery and resolves to `Unresolved_<ip>`. The failure is completely silent: events still persist, the feed still scrolls, no exception is raised — but every asset name is wrong, every CII is zero (an `Unresolved_` node has no dependency-graph edges), and no alert can ever name a real asset. Pinned by `test_default_registry_resolves_curated_assets`, verified to genuinely fail under deliberate sabotage (7 tests fail when the fix is reverted) |
| P5-17 | CII is computed for the anomaly itself, NOT gated behind the alert decision; and is skipped entirely for an origin asset absent from the dependency graph | Two separate calls. (a) Gating CII on alerting would mean suppressing the noisy volumetric channel also silently discards every blast radius — but a `CiiSnapshot` is an analytical record in its own right (`backend/models.py` deliberately lets it survive its trigger event's deletion). (b) An asset that is not a graph node has no edges to propagate along, so `compute_cascading_impact_full()` returns an all-zero `CIIResult` **by construction** — running `mc_iterations` Monte Carlo passes to obtain a known-in-advance zero is pure waste, and at real scale it is thousands of wasted simulations (risk T5: one auto-registered node per unique unresolved IP). The guard makes the gap visible instead of filling the table with fake zero-valued snapshots |
| P5-18 | `GET /api/events` orders `id ASC` ALONE when `since` is supplied and `ts DESC, id DESC` only when it is omitted — superseding D8-2's unconditional DESC (HIGH-1) and then HIGH-1's own `ts ASC, id ASC` catch-up ordering (HIGH-2), both of which this decision now corrects | Ticket #8 review finding HIGH-1, then a second finding HIGH-2 found by live end-to-end verification against a running uvicorn server + real Postgres, after HIGH-1 had already been accepted. **HIGH-1:** `WHERE id > :since ORDER BY ts DESC, id DESC LIMIT n` is a backward-looking "latest N" window applied to a forward cursor; a client more than `limit` events behind advances its cursor past a range it never received and can never recover it. Measured on real Postgres (250 events sharing one `ts`, ids 5067..5316, cursor at 5067, `limit=100`): 149 of 249 events permanently lost across 3 polls, with the `{events: [...]}` envelope giving the client no way to even detect it. Fixed at the time by flipping the catch-up branch to `ts ASC, id ASC`. **HIGH-2 (this addendum):** that fix was still wrong, because it sorted by `ts` while filtering by `id` — two different keys — and cursor pagination is only gapless when the filter key and the sort key are the *same* column. A replayed capture day proves id order and ts order can disagree: a second replay session gets higher ids than the first but restarts its own virtual clock, so its rows can carry an earlier or overlapping `ts`. Measured on real Postgres, three replay sessions of one day: session ids 5062..5066 (ts 14:29..14:30), 5567..11566 (ts 14:29..14:41), and 11567 alone (ts 14:29 — the earliest ts of the three, but the highest id). `since=10567&limit=5` sorted id 11567 to the front (earliest ts), so a client draining forward either permanently skipped ids 10568..11566 (advancing by `max(id)`) or re-received 11567 forever (advancing by sort position) — measured end-to-end at **200 of 6006 events delivered, 96.7% lost**, worse than HIGH-1. Fixed by making the catch-up branch `ORDER BY id ASC` alone — filter key and sort key identical, gapless by construction; the page-load branch is untouched. The `id` tie-break from the original D8-2 was correct throughout and is unchanged. `EventsResponse.has_more` (unchanged, honestly computed via `limit + 1`-then-trim) still lets a client know when it has caught up. See docs/PHASE5_TICKET8_PLAN.md section 4's two correction notes and the Ticket #8 row above |

Also fixed in the same review pass (MEDIUM-2): `_emit_batch` previously incremented `emitted_count` unconditionally, so `status().emitted_count` did not distinguish flows the consumer successfully processed from flows in a batch whose consumer call raised. `ReplayStatus` now also exposes `consumer_failed_flow_count` (flows, incremented alongside the existing batch-level `consumer_error_count`), with the subtraction relationship (`emitted_count - consumer_failed_flow_count` = successfully processed) made explicit in the docstring.

---

## Known issues (recorded, not blocking)

| ID | Issue | Disposition |
|---|---|---|
| K1 | `_instance: ClassVar` in `backend/config.py` is declared but never used | Mirrors the same dead pattern at `src/settings.py:280`. Consistent with codebase; not worth sprint time. |
| K2 | `model_artifact_path` (unresolved) still exists alongside `model_artifact_path_resolved` | A caller could use the CWD-relative one by mistake. **Ticket #5 must use `model_artifact_path_resolved`.** |
| K3 | `docs/ARCHITECTURE.md` is referenced by the execution directive but was deleted in Phase 3 cleanup (described the superseded pre-gateway BFS design) | Recon §0 records this. Ticket #18 rewrites architecture docs. |
| K4 | `HONEYTOKEN_CREDENTIALS` defines 6 gateway zones (L0–L5) but `graph_manager.gateway_nodes()` materialises only 4 (L1/L3/L4/L5), because Traffic_Cam (0.2) and SCADA_Historian (0.6) fall below the 0.85 protection threshold, so L0/L2 guard nothing | Consequence: the **Camera Spoofing** and **Data Exfiltration** scripted attacks recon against gateways with no graph node and an empty protected set. **Payment Gateway Breach (L4) and Lateral Movement (L3) are unaffected — the headline demo path is sound.** Pre-existing Phase 2 drift; fixing it means touching `src/` (Invariant A), so it is recorded, not silently worked around. Phase 5 should demo the L4 path. |
| K5 | `ml_engine.train_isolation_forest` does `contamination=contamination or SETTINGS.ml.isolation_forest_contamination` — passing `contamination=0.0` is silently treated as "not given" and falls back to 0.08, NOT "flag nothing" | `BACKEND_SETTINGS.streaming_contamination` is bounded `gt=0.0` specifically to make this trap unreachable through `StreamingScorer`; pinned by `tests/test_streaming_scorer.py::test_contamination_zero_silently_ignored_by_ml_engine`. Fixing the `or` itself means touching `src/` (Invariant A), so it is recorded, not fixed. |
| K6 | A supervised detector (e.g. `backend.supervised_detector.SupervisedFlowDetector`) must **never** be added to the global `detectors.registry.DETECTORS` via `register_detector()` | Found in Ticket #5 review as HIGH-1 and fixed: `detectors.registry`'s contract is "registered ⇒ benchmarked" — `evaluation.run_evaluation()` (`src/evaluation/__init__.py`, frozen, Invariant A) iterates every registered non-tripwire entry and fits it on a BENIGN-ONLY, label-free split, then publishes precision/recall/F1 into the Research Console's benchmark table. A supervised classifier is not a member of that unsupervised volumetric benchmark family, so global registration produces a meaningless garbage row (measured: `Supervised_Flow 0.000 0.000 0.000 0.500 0`) by construction, not by bug — identical in kind to why `_BENCHMARK_EXCLUDED_DETECTORS` already excludes `tripwire`. Expose any future supervised/labelled detector through a backend-local map (see `backend.supervised_detector.BACKEND_DETECTORS`) or direct import instead. Adding such a detector to `_BENCHMARK_EXCLUDED_DETECTORS` in `src/evaluation/__init__.py` would be the alternative fix but requires editing `src/`, which Invariant A forbids from outside its owning ticket. |
| K7 | Tests that write to the repo's real `artifacts/` path must preserve and restore any pre-existing artifact, never unconditionally delete it | Found in Ticket #5 final verification as MEDIUM-1. The warmup artifact is a **build-time** product (`python -m backend.warmup`, ~5.3 s, 823 KiB) that the Operations Console loads at startup and deliberately will **not** regenerate on demand — `load()` hard-fails rather than refitting, because a silent refit on stream data is exactly the Invariant B violation this phase exists to prevent. So any test that deletes it converts "ran the tests" into "demo is broken", with no error until startup. Pattern: capture `read_bytes()` if the file exists, restore it in a `finally`, and `unlink(missing_ok=True)` only when it did not exist beforehand. |
| K8 | **Real replayed CIC-IDS2017 traffic resolves to ZERO dependency-graph assets**, so the ambient (non-injected) stream produces no CII snapshots and no blast radius | Found by Ticket #7's end-to-end verification and then measured directly: over 20,000 real friday-morning flows, **0 of 20,000 (0.00%)** source IPs resolve to a node in `build_graph()`. Cause: real CIC-IDS2017 hosts are `192.168.10.x` (plus external `172.217.x`, `52.84.x`, …), while every curated smart-city asset is `10.0.1.x`, and `AssetRegistry`'s subnet-proximity heuristic only covers the `10.0.1.x` range. This **contradicts `docs/PHASE5_BUILD_PLAN.md` §5**, which asserts real IPs map onto curated assets via `AssetRegistry.resolve()` — *"already built, already tested"*. That assumption is false for this dataset. **The demo's headline path is NOT affected**: Ticket #13's injection uses `generate_scripted_attack`, whose `exfil_edge` carries asset *names* (not IPs), so injected attacks name real graph nodes and the CII cascade fires normally. What is affected is ambient traffic, which is arguably honest — those really are unmodeled external/LAN hosts with no curated dependency to cascade through. **Ticket #11 must decide** whether `/24` cluster nodes get synthetic graph edges or whether ambient traffic is left deliberately CII-free; fixing `AssetRegistry`'s heuristic itself would touch `src/` (Invariant A). |

---

## Timing / demo notes

- friday-morning spans 08:59–12:59 (191,033 rows). At 20× that is ~12 minutes of demo. First real attack lands at 09:34 — about **1.5 minutes into the stream** — and attack density peaks 10:30–11:00 at 4.31%. Useful for Ticket #6 if a start-offset is wanted.
- Full read+sort cost measured: wednesday (692,703 rows) **28.3 s, ~380 MiB**; friday-morning (191k) proportionally less. Ticket #6 must account for this at startup.

---

## Note for Ticket #8 (recorded by Ticket #6)

Because hundreds of events can share one `ts` on minute-granularity days
(friday-morning: median 629 events/bucket, max 4,017), any "recent events"
API query **must order by `ts DESC, id DESC`** — `ts` alone is ambiguous
within a bucket and would make the live feed appear jumbled. The serial
`id` primary key preserves true emission (insert) order within a tied
timestamp, since `ReplayEngine` emits flows within a bucket in the
deterministic order `compute_virtual_times()`/`ReplayFlowReader`'s stable
sort produced (P5-7), and Ticket #7's consumer is expected to insert them
in the order they arrive in each batch.

**Update from Ticket #8 (review HIGH-1):** the `ts DESC, id DESC` /
`id`-breaks-ties requirement above is now implemented in
`GET /api/events` — but **only for a page load with `since` omitted**.
Applying that same DESC ordering to a `since`-bounded query turns out to
be wrong, not merely incomplete: it makes the query backward-looking
against a forward cursor, and a client that falls more than `limit`
events behind silently and permanently loses the gap in between —
measured at 149 of 249 events lost across 3 polls on a 250-row tied-`ts`
batch, real Postgres. The cursor direction had to be **inverted** for the
`since`-supplied case: `ts ASC, id ASC`, so the client drains forward with
no gap. The `id` tie-break itself is unchanged and still applies in both
directions.

**Second update from Ticket #8 (HIGH-2, found by live end-to-end
verification after HIGH-1 was already accepted):** `ts ASC, id ASC` for
the catch-up branch was still wrong. The filter is `WHERE id > :since`
but that ordering sorts by `ts` first — a different key than the filter
— and cursor pagination is only gapless when the filter key and the sort
key are the same column. A capture day replayed a second time proves id
order and ts order can disagree (the second session's rows get higher
ids but restart the virtual clock, so they can carry an earlier or
overlapping `ts`); measured end-to-end, this delivered 200 of 6006
events (96.7% lost) before permanently skipping the rest or looping on a
duplicate, depending on how the client advances its cursor. The catch-up
branch is now `ORDER BY id ASC` **alone** — no `ts` in it at all — so the
filter key and sort key are identical and the drain is gapless by
construction, and it matches this note's own original point: the serial
`id` is what actually preserves true emission order, so it is the
correct catch-up key, full stop. **Ticket #10's live-feed author:** do
not reuse this route's page-load `ts DESC, id DESC` ordering for a
polling/catch-up path, and do not assume `since`'s ordering has anything
to do with `ts` — use `since` and rely on the id-ascending order plus the
`has_more` field to know when to stop. See decision P5-18 (and its
addendum) and docs/PHASE5_TICKET8_PLAN.md section 4's two correction
notes.

---

## Note for Ticket #11 (recorded by Ticket #7)

`/24` cluster aggregation is now load-bearing for more than readability.
Measured: **0 of 20,000** real friday-morning source IPs resolve to a
dependency-graph node (K8), so today every ambient event lands on an
auto-registered `Unresolved_192_168_10_x` node with criticality 0.1 and no
edges. Two consequences for Ticket #11:

1. The graph view driven by ambient traffic has nothing to draw *inside*
   the curated topology — the real volume is entirely outside it.
2. `IngestPipeline` deliberately skips the CII Monte Carlo for any origin
   asset absent from the graph (P5-17), so cluster nodes will produce no
   blast radius unless Ticket #11 gives them graph edges.

Deciding between "cluster nodes get synthetic edges into the topology" and
"ambient traffic is deliberately CII-free, and CII belongs to injected
scenarios only" is Ticket #11's call. The second option is defensible and
cheaper — and it is what the current code does.

---

## Live risks carried forward

| ID | Risk | Current mitigation |
|---|---|---|
| T3/T3b | CIC timestamps: per-file precision differs; **all files use a 12-hour clock with no AM/PM**, so a naive sort mis-orders afternoon traffic | Ticket #5b must apply the deterministic AM/PM correction (hours 1–7 → +12) and assert per-file monotonicity in a test |
| T5 | `AssetRegistry` auto-registers one node per unique IP → thousands of nodes | `/24` cluster aggregation must live in the Phase 5 layer (Ticket #11), **not** by modifying `AssetRegistry` (Invariant A) |
| T-B | Accidental model refit in the streaming path | **Resolved (Ticket #5).** `StreamingScorer` fits once in `fit_from_warmup()`; `score_batch`/`score_event`/`explain` call `transform()`/`decision_function()` only. Pinned by two tests: `test_invariant_b_scaler_identity` (scaler object identity + byte-identical `mean_`/`scale_`/`var_` across 51 batches incl. an extreme-outlier batch) and the stronger `test_invariant_b_no_fit_calls_in_scoring_path` (monkeypatches `StandardScaler.fit`/`fit_transform`, `IsolationForest.fit`, and `ml_engine.preprocess_features` to raise — catches even a refactor that swaps in a *fresh* scaler, which the identity test alone would miss) |
| T11 | Disk exhaustion | Resolved for now (~160 GB free); recheck before `node_modules` lands in Ticket #3 |

---

## Ticket #1 — acceptance record

**Verdict: PASS.** Verified independently by Opus, not accepted on the implementer's report.

Delivered: `backend/__init__.py`, `backend/config.py`, `backend/db_check.py`,
`requirements-backend.txt`, `tests/test_backend_config.py` (18 tests);
modified `.env.example`, `.gitignore`. Nothing under `src/`.

Review found 3 defects, all fixed and re-verified:

| Severity | Finding | Verified failure before fix | After fix |
|---|---|---|---|
| **HIGH** | `database_url` did not URL-encode credentials | `AEGIS_DB_PASSWORD='p@ss:w/rd'` → `make_url()` raised `ValueError: invalid literal for int() with base 10: 'w'` | `p@ss:w/rd#x?y` percent-encodes and round-trips to the exact original host/user/password/port/db |
| **HIGH** | `api_host` defaulted to `0.0.0.0` — unauthenticated API + WebSocket, incl. replay/injection control, exposed to the whole LAN | n/a (design defect) | defaults to `127.0.0.1`; `0.0.0.0` documented as deliberate opt-in |
| **MEDIUM** | `model_artifact_path` resolved against CWD | from `/tmp` → `/private/tmp/artifacts/streaming_scorer.joblib` | identical absolute path from repo root and `/tmp` |

Also hardened `db_check._redact` to parse via `make_url(...).render_as_string(hide_password=True)`
instead of a regex — verified no leak for a password containing `@`.

Acceptance criteria:

- [x] Postgres reachable from the backend's own config path (`python -m backend.db_check`)
- [x] Typed, validated, frozen settings singleton; env + `.env` overridable
- [x] Single-URL escape hatch (`AEGIS_DATABASE_URL`) takes precedence, passed through verbatim
- [x] No secrets committed; `.env` gitignored, only `.env.example` tracked
- [x] Backend tests pass with **no live database** (CI has none)
- [x] **247 passed, 0 failed** — engine baseline of 229 intact
- [x] Ruff clean; duplicate-def check clean
- [x] Invariant A: `src/` untouched
| 17 | SDG 9 / SDG 11 framing | — | ✅ **DONE** — folded into the README rewrite (#18) rather than kept as a separate bolt-on section, since the SDG claim only carries weight when tied to the actual asset model and the honesty posture. SDG 9 argues from the explicit cascading-dependency model and the Purdue gateway segmentation; SDG 11 from the assets actually modelled (water, power, traffic, emergency dispatch, hospital, and welfare disbursement alongside the grid). States outright that an overstated detector makes infrastructure decisions worse, not better |
| 18 | README rewrite | — | ✅ **DONE.** The old README was materially false: it declared the FastAPI service, WebSocket streaming, ingest pipeline and Next.js frontend "**not** built yet" when all four are working. Rewritten around what is actually true and verified: 50-node city / 11 sectors / 75 edges, 12 REST routes, 3.2 GB of real capture across 8 days, 538 tests. Adds an explicit **Honest limitations** section publishing the unfavourable measurements — unsupervised precision ~0.02 (5 TP vs 811 FP), supervised precision 0.000 on novel families, K8's 0-of-20,000 asset-resolution finding, and why a CII median of 0.0 is truthful. **Caught while writing:** the 58.4 s tripwire lead time, carried forward from the old README, is measured on `generate_scripted_attack()` timelines — the synthetic generator Ticket #13 deliberately refused — not on real capture. Kept, because the reason is legitimate (`is_honeytoken_use` is false on every row of a 2017 public capture by definition, so the ordinary P/R harness would trivially predict "normal"), but now qualified in both places it appears rather than propagated unmarked |
