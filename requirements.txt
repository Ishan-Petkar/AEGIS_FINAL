# AEGIS — Python dependencies
# Pin major versions; allow minor/patch upgrades via >=x.y,<x+1.0
# Generated from the project venv — run `pip install -r requirements.txt` to reproduce.

# --- Core ML & Data ---
numpy>=2.4,<3.0
pandas>=3.0,<4.0
scikit-learn>=1.8,<2.0

# --- Graph ---
networkx>=3.6,<4.0

# --- Settings / Config ---
pydantic>=2.0,<3.0

# --- Visualization / UI ---
plotly>=6.7,<7.0
streamlit>=1.57,<2.0

# --- Testing & CI ---
pytest>=9.0,<10.0
pytest-cov>=7.0,<8.0
